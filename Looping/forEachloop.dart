void main(){
  //format collection.forEach(void f(value))
  List <String> countries = ["Kenya","Somali","Malawi","Tanzania"];
  // collection is the list the void with the 
  countries.forEach( (name)=>print(name)); 
}